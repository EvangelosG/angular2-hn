{
  "PreToolUse": [
    {
      "matcher": "exec",
      "hooks": [
        {
          "type": "command",
          "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); c=d.get('tool_input',{}).get('command',''); blocked=any(p in c for p in ['rm -rf', 'git push --force', ':(){']); print(json.dumps({'decision':'block','reason':'Blocked by .devin/hooks.v1.json policy: destructive command detected.'}) if blocked else '', end=''); sys.exit(2 if blocked else 0)\""
        }
      ]
    }
  ]
}
